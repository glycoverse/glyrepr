# structure mappers validate floating metadata returned by callbacks

    Code
      smap_structure(glycan, delete_floating_parts_attr)
    Condition
      Error in `purrr::map()`:
      i In index: 1.
      Caused by error in `validate_floating_graph_shape()`:
      ! Glycan structure must be an out tree.

