% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure-tables.R
\name{structure_component_membership}
\alias{structure_component_membership}
\title{Identify Main and Floating Structure Components}
\usage{
structure_component_membership(x)
}
\arguments{
\item{x}{A glycan structure vector.}
}
\value{
A tibble with columns \code{glycan_id}, \code{node_id}, \code{component_type},
and \code{part_id}, plus \code{glycan_name} when \code{x} is named.
}
\description{
\code{structure_component_membership()} identifies whether each glycan node
belongs to the main tree or to a floating part. It provides a public,
normalized alternative to reading the private floating-part graph
attribute.

Main-tree nodes have \code{component_type = "main"} and a missing \code{part_id}.
Nodes in a floating subtree have \code{component_type = "floating"} and the
corresponding \code{part_id} from \code{\link[=structure_floating_parts]{structure_floating_parts()}}.

Node indices refer to \code{structure_nodes()$node_id} for the same glycan.
Missing structures contribute no rows. Duplicate structures are expanded to
their original vector positions. If \code{x} is named, the result also contains a
\code{glycan_name} column.
}
\examples{
glycan <- as_glycan_structure(
  "{Neu5Ac(a2-6)|1,2}Gal(b1-3)GalNAc(a1-"
)
structure_component_membership(glycan)

}
