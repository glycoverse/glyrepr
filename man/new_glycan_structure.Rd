% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure-graph-low-level.R
\name{new_glycan_structure}
\alias{new_glycan_structure}
\title{Construct a Glycan Structure Vector from Trusted Data}
\usage{
new_glycan_structure(iupac = character(), graphs = list())
}
\arguments{
\item{iupac}{A character vector of canonical IUPAC-condensed strings. Missing
values are allowed, and names are preserved exactly.}

\item{graphs}{A named list of valid, canonical, mutually compatible \code{igraph}
glycan graphs keyed by IUPAC-condensed strings.}
}
\value{
A \code{glyrepr_structure} vector.
}
\description{
Assemble a glycan structure vector from IUPAC-condensed values and a graph
lookup table without graph validation, canonicalization, IUPAC generation,
vector-level compatibility checks, or graph deduplication.
}
\details{
\code{graphs} must be a named list containing one graph for every distinct,
non-missing value in \code{iupac}. Additional named graphs are allowed so that
vctrs prototypes can retain their graph lookup tables. Graph names must be
unique and non-missing. This function checks these inexpensive representation
invariants but trusts that each graph matches its key.
}
\section{Low-level API warning}{

These functions are low-level, developer-facing APIs. Calling them directly
is usually not a good idea unless you understand and can guarantee all glycan
graph and \code{glyrepr_structure} invariants. Prefer \code{\link[=as_glycan_structure]{as_glycan_structure()}} for
ordinary construction. Incorrect use of these functions can create invalid
structure vectors that fail in later operations.
}

\section{Name-preserving manual construction}{

The five low-level functions can reproduce strict graph-based construction
while preserving the names of the input graph list:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{input_names <- names(graphs)
graphs <- unname(graphs)

graphs <- purrr::map(graphs, validate_glycan_graph)
graphs <- purrr::map(graphs, canonicalize_glycan_graph)
validate_glycan_graph_vector(graphs)

iupacs <- purrr::map_chr(graphs, graph_to_iupac)
names(iupacs) <- input_names

unique <- !duplicated(unname(iupacs))
unique_graphs <- graphs[unique]
names(unique_graphs) <- unname(iupacs[unique])

new_glycan_structure(iupacs, unique_graphs)
}\if{html}{\out{</div>}}

Unlike \code{as_glycan_structure(graphs, on_failure = "na")}, this strict
pipeline stops at the first invalid graph.
}

\seealso{
Other low-level glycan structure functions: 
\code{\link{canonicalize_glycan_graph}()},
\code{\link{graph_to_iupac}()},
\code{\link{validate_glycan_graph}()},
\code{\link{validate_glycan_graph_vector}()}
}
\concept{low-level glycan structure functions}
