% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure-level.R
\name{get_structure_level}
\alias{get_structure_level}
\title{Get the Structure Resolution Levels}
\usage{
get_structure_level(x)
}
\arguments{
\item{x}{A \code{\link[=glycan_structure]{glycan_structure()}} vector or a glycan \code{igraph}.}
}
\value{
For vector input, a character vector containing one structure level
per element. Missing elements return \code{NA_character_}, and an empty input
returns \code{character()}. For graph input, a character scalar.
}
\description{
Glycan structures can have three possible levels of resolution, determined
only by linkage and anomer information:
\itemize{
\item "intact": No linkage or anomer is unknown or ambiguous.
\item "partial": At least one linkage or anomer is unknown or ambiguous, and
at least one linkage or anomer contains known information.
\item "topological": All linkages and anomers are completely unknown
("??-?"/"??").
}

Floating metadata does not by itself affect the structure level. A floating
part's attachment linkage is evaluated like an ordinary linkage, while
candidate-parent ambiguity for floating parts and substituents is independent
of linkage resolution. Consequently, a glycan with floating metadata is
regarded as "intact" when every graph-edge and floating-part attachment
linkage, as well as the reducing-end anomer, is fully specified, even though
a parent residue remains unlocalized.
}
\examples{
glycan <- as_glycan_structure("Gal(b1-3)GalNAc(a1-")
get_structure_level(glycan)

floating <- as_glycan_structure(
  "{Neu5Ac(a2-6)|2,3}Gal(b1-3)GalNAc(a1-"
)
get_structure_level(floating)

}
\seealso{
\code{\link[=has_linkages]{has_linkages()}}, \code{\link[=has_floating_parts]{has_floating_parts()}},
\code{\link[=has_floating_substituents]{has_floating_substituents()}}, \code{\link[=get_mono_type]{get_mono_type()}}
}
