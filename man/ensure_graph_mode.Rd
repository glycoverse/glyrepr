% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert-graph-mode.R
\name{ensure_graph_mode}
\alias{ensure_graph_mode}
\title{Ensure Glycan Graph is in the Correct Mode}
\usage{
ensure_graph_mode(glycan, mode)
}
\arguments{
\item{glycan}{A glycan graph.}

\item{mode}{A character string specifying the mode of the glycan graph.
Can be "ne" or "dn". See docs for \code{\link[=as_glycan_graph]{as_glycan_graph()}} for more details.}
}
\value{
The glycan graph in the correct mode.
}
\description{
This function ensures that a glycan graph is in the correct mode (NE or DN).
It is a wrapper around \code{\link[=convert_ne_to_dn]{convert_ne_to_dn()}} and \code{\link[=convert_dn_to_ne]{convert_dn_to_ne()}}.
If the glycan graph is already in the correct mode, it is returned as is,
otherwise it is converted to the correct mode.
This function is handy when you are not sure about the mode of the glycan graph.
It eliminates the need to check the mode manually before converting.
}
\examples{
some_ne_glycan <- n_glycan_core(mode = "ne")
is_ne_glycan(ensure_graph_mode(some_ne_glycan, "ne"))
is_dn_glycan(ensure_graph_mode(some_ne_glycan, "dn"))

}
\seealso{
\code{\link[=convert_ne_to_dn]{convert_ne_to_dn()}}, \code{\link[=convert_dn_to_ne]{convert_dn_to_ne()}}
}
