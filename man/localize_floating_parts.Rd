% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/floating-localization.R
\name{localize_floating_parts}
\alias{localize_floating_parts}
\title{Localize Floating Glycan Parts}
\usage{
localize_floating_parts(x, assignments)
}
\arguments{
\item{x}{A glycan structure vector or a glycan \code{igraph}.}

\item{assignments}{A data frame with integer columns \code{glycan_id}, \code{part_id},
and \code{parent_node}. Each \verb{(glycan_id, part_id)} pair may occur at most once.}
}
\value{
An object of the same representation as \code{x}. Structure-vector
output has the same length and names as \code{x}; graph output retains its
vertex IDs and order.
}
\description{
\code{localize_floating_parts()} attaches selected floating parts to
caller-supplied parent nodes. The assignments are interpreted against the
floating-part rows and canonical node identifiers returned by
\code{\link[=structure_floating_candidates]{structure_floating_candidates()}}.

Each selected parent must belong to the floating part's declared candidate
domain. For an unrestricted \verb{\{<floating>\}} part, that domain contains every
node in the original main tree. Assignments must also be simultaneously
compatible with occupied and potential acceptor linkage positions.

Selected virtual attachments become ordinary graph edges. Unassigned
floating parts remain floating. The resulting structures are canonicalized,
and candidate-parent indices for remaining parts are remapped to the new
canonical main-tree order.

Missing values, vector positions, and names in structure-vector input are
preserved. For graph input, \code{glycan_id} must be \code{1L}, and selected edges are
appended without canonicalizing or renumbering vertices.
}
\examples{
glycan <- as_glycan_structure(
  "{Neu5Ac(a2-6)|1,2}Gal(b1-3)GalNAc(a1-"
)
assignments <- tibble::tibble(
  glycan_id = 1L,
  part_id = 1L,
  parent_node = 2L
)
localize_floating_parts(glycan, assignments)

}
