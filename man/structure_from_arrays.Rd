% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure-arrays.R
\name{structure_from_arrays}
\alias{structure_from_arrays}
\title{Construct Structures from Residue and Edge Arrays}
\usage{
structure_from_arrays(x, on_failure = c("error", "na"))
}
\arguments{
\item{x}{A list of structure records. A \code{NULL} element represents a missing
structure. Names, missing positions, and duplicate positions are preserved.}

\item{on_failure}{Either \code{"error"} (default) or \code{"na"}. The latter warns and
returns missing structures at invalid positions.}
}
\value{
A \code{glyrepr_structure} vector, with graph storage deduplicated by
canonical IUPAC key.
}
\description{
A format-independent entry point for parsers. Node IDs are one-based positions
in \code{mono}; edges point from parent to child. Input order need not be canonical.
Each record contains \code{mono}, \code{sub}, \code{edges}, \code{linkage}, and \code{anomer}.
\code{edges} is an interleaved integer vector \code{c(parent, child, ...)}.
\code{sub} contains comma-separated substituent tokens (or empty strings).
Optional \code{alditol} defaults to \code{FALSE}.
}
\details{
Optional \code{floating_parts} is a list of records containing \code{root}, \code{nodes},
\code{linkage}, and \code{parents}. Each \code{nodes} vector must contain exactly the nodes
in that disconnected component. Optional \code{floating_substituents} is a list of
records containing \code{substituent} and \code{parents}. Empty \code{parents} means all
feasible nodes; all indices refer to the original input arrays. Singleton
candidates are resolved during canonicalization.

Records above the native backend's size guard use the graph reference path;
the guard is not an input-size limit. Only failing or unsupported records
use that path. No format-specific strings are generated and reparsed.
Failures have class \code{glyrepr_error_structure_failure} and fields \code{position},
\code{input_name}, and \code{reason}. Recovery warnings have class
\code{glyrepr_warning_structure_failure} and fields \code{positions} and \code{reasons}.
}
\examples{
structure_from_arrays(list(list(
  mono = c("Glc", "Gal"), sub = c("", ""),
  edges = c(1L, 2L), linkage = "b1-4", anomer = "?1"
)))
}
