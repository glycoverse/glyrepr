% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/floating-substituents.R
\name{has_floating_substituents}
\alias{has_floating_substituents}
\title{Detect Floating Substituents}
\usage{
has_floating_substituents(x)
}
\arguments{
\item{x}{A \code{\link[=glycan_structure]{glycan_structure()}} vector or a glycan \code{igraph}.}
}
\value{
A logical vector with the same length and names as vector input, or
a logical scalar for graph input. Missing structures produce \code{NA}.
}
\description{
Test whether each glycan structure contains one or more substituents whose
parent residue is unresolved.
}
\details{
In the \code{glyrepr} IUPAC extension, floating substituents appear in braces
before the main glycan. \verb{\{6S\}<main>} allows every feasible residue in the
complete structure to carry the substituent, \verb{\{6S|1,2\}<main>} restricts it
to complete-sequence nodes 1 and 2, and \verb{\{?S\}<main>} also leaves the carbon
position unknown. Residue nodes in floating blocks are counted before the
main glycan; substituent blocks contribute no node indices.

Internally, unresolved substituents are stored in the
\code{floating_substituents} graph attribute. It is a list with one entry per
substituent. Each entry contains a canonical \code{substituent} token and an
integer \code{parents} vector of candidate graph vertex indices. An empty vector
means every feasible residue vertex is a candidate.

A singleton candidate set is normalized into that vertex's ordinary \code{sub}
attribute. This also happens whenever chemistry leaves only one feasible
residue in the complete structure.
\code{\link[=structure_floating_substituents]{structure_floating_substituents()}} exposes the metadata as a normalized
table.
}
\examples{
glycans <- as_glycan_structure(c(
  floating = "{6S}Gal(a1-3)Gal(a1-",
  restricted = "{6S|1,2}Gal(a1-3)Glc(a1-3)Man(a1-",
  ordinary = "Gal6S(a1-"
))
has_floating_substituents(glycans)

}
\seealso{
\code{\link[=structure_floating_substituents]{structure_floating_substituents()}}, \code{\link[=has_floating_parts]{has_floating_parts()}},
\code{\link[=as_glycan_structure]{as_glycan_structure()}}
}
