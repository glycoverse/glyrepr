% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/floating-localization.R
\name{enumerate_floating_graph_localizations}
\alias{enumerate_floating_graph_localizations}
\title{Enumerate Floating-Part Graph Localizations}
\usage{
enumerate_floating_graph_localizations(graph, max_variants = 256)
}
\arguments{
\item{graph}{A valid glycan \code{igraph}, optionally containing unresolved
floating parts.}

\item{max_variants}{A positive integer giving the maximum raw candidate
combinations allowed.}
}
\value{
A tibble with columns:
\itemize{
\item \code{variant_id}: the sequential localization identifier.
\item \code{graph}: a list-column of fully localized \code{igraph} objects whose vertex
IDs are identical to those in \code{graph}.
\item \code{assignments}: a list-column of tibbles with \code{glycan_id}, \code{part_id}, and
\code{parent_node}. \code{glycan_id} is always \code{1L}.
}
}
\description{
\code{enumerate_floating_graph_localizations()} generates every conflict-free,
fully localized graph permitted by the candidate-parent domains in \code{graph}.
It is the graph-level counterpart to \code{\link[=enumerate_floating_localizations]{enumerate_floating_localizations()}}.

The returned graphs are validated but are not canonicalized. Adding the
selected attachment edges does not reorder vertices, so every vertex keeps
the same integer ID, name, and attributes as in \code{graph}. Assignment
\code{parent_node} values therefore refer directly to vertex IDs in both the
input and localized graphs.

Every conflict-free assignment is retained, even when multiple assignments
would produce the same canonical IUPAC-condensed structure. A graph without
floating parts produces one identity row with an empty assignment table.

\code{max_variants} is a conservative safeguard. It limits the raw Cartesian
product before conflict filtering, so the function may ask for a higher
bound even when fewer variants would ultimately remain.
}
\section{Low-level API warning}{

These functions are low-level, developer-facing APIs. Calling them directly
is usually not a good idea unless you understand and can guarantee all glycan
graph and \code{glyrepr_structure} invariants. Prefer \code{\link[=as_glycan_structure]{as_glycan_structure()}} for
ordinary construction. Incorrect use of these functions can create invalid
structure vectors that fail in later operations.
}

\section{Floating graph schema}{

A floating structure is one weakly disconnected graph with exactly one main
outward tree and one outward tree per floating part. Its \code{floating_parts}
graph attribute is a list of entries with integer \code{root}, integer \code{nodes},
character \code{linkage}, and integer \code{parents} fields. \code{nodes} contains every
vertex in that floating component. \code{parents = integer()} means all feasible
main-tree nodes. Legacy input graphs may omit \code{nodes}; canonical output
graphs always contain it. During canonicalization, a part with exactly one
effective candidate parent is converted to an ordinary graph edge and its
floating metadata is removed. Otherwise, the virtual attachment linkage is
not a graph edge. See \code{\link[=glycan_structure]{glycan_structure()}} for the complete contract.
}

\section{Name-preserving manual construction}{

The five low-level functions can reproduce strict graph-based construction
while preserving the names of the input graph list:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{input_names <- names(graphs)
graphs <- unname(graphs)

graphs <- purrr::map(graphs, validate_glycan_graph)
graphs <- purrr::map(graphs, canonicalize_glycan_graph)
validate_glycan_graph_vector(graphs)

iupacs <- purrr::map_chr(graphs, graph_to_iupac)
names(iupacs) <- input_names

unique <- !duplicated(unname(iupacs))
unique_graphs <- graphs[unique]
names(unique_graphs) <- unname(iupacs[unique])

new_glycan_structure(iupacs, unique_graphs)
}\if{html}{\out{</div>}}

Unlike \code{as_glycan_structure(graphs, on_failure = "na")}, this strict
pipeline stops at the first invalid graph.
}

\examples{
glycan <- as_glycan_structure(
  "{Neu5Ac(a2-6)|1,2}Gal(b1-3)GalNAc(a1-"
)
graph <- get_structure_graphs(glycan, return_list = FALSE)
localizations <- enumerate_floating_graph_localizations(graph)
localizations$graph

}
\seealso{
\code{\link[=enumerate_floating_localizations]{enumerate_floating_localizations()}}
}
