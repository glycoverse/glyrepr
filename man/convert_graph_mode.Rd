% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert-graph-mode.R
\name{convert_graph_mode}
\alias{convert_graph_mode}
\title{Convert between NE and DN glycan graphs}
\usage{
convert_graph_mode(glycan, to, strict = TRUE)
}
\arguments{
\item{glycan}{A glycan graph.}

\item{to}{The target graph mode, either "ne" or "dn".}

\item{strict}{If \code{TRUE}, an error will be thrown if the glycan is already
in the target mode. If \code{FALSE}, the glycan will be returned as is.
Default is \code{TRUE}.}
}
\value{
The converted glycan graph object.
}
\description{
This function converts a Node-Edge (NE) glycan graph into a Dual-Node (DN) glycan graph,
or vice versa.
In an NE glycan graph, nodes represent monosaccharides and edges represent linkages.
In a DN glycan graph, both monosaccharides and linkages are nodes, and they alternate.
See \code{\link[=as_glycan_graph]{as_glycan_graph()}} for details.
}
\examples{
(glycan <- n_glycan_core(mode = "ne"))

# Convert to DN mode
convert_graph_mode(glycan, to = "dn")

# Converting to NE mode will raise an error
try(convert_graph_mode(glycan, to = "ne"))

# Set `strict` to `FALSE` to return the glycan unchanged
convert_graph_mode(glycan, to = "ne", strict = FALSE)

}
